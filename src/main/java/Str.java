public class Str {
    public static void main(String[] args) {
        String str ="내일은 맛있는 학식이 나오면 좋겠는데용..과연 무엇이 나올까요?";

        int len = str.length();

        System.out.printf("문자열: %s \n", str);
        System.out.printf("문자열 길이: %d", len);
    }
}
