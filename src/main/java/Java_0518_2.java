public class Java_0518_2 {
    public static void main(String[] args) {
        String str = "IT CookBook.Java";
        int len = str.length(); //length 문자열 길이, 중복

        System.out.printf("문자열: %s \n", str);
        System.out.printf("문자열 길이: %d", len);

    }
}
